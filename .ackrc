# User ack options. Some of these could be perhaps moved to project-specific
# config files with the advent of ack 2. See:
#     http://beyondgrep.com/documentation/ack-2.08-man.html#ackrc_location_semantics

# ignore case if pattern contains no uppercase chars
--smart-case

# set colors to be more visible on a light terminal bg:
--color-filename=blue
--color-lineno=yellow

# define some types:
--type-set=ftl=.ftl
--type-set=prop=.properties
--type-set=feature=.feature
--type-set=jsp=.jsp,.jspx,.jhtm,.jhtml,.tag
--type-set=groovy=.groovy,.gradle
--type-set=haml=.haml

# built-in types that I'd like to make a bit broader:
--type-set=css=.css,.scss,.less

# ignore build artifacts by default:
--ignore-dir=classes
--ignore-dir=build
